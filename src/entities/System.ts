export interface System {
  icon: string;
  id: string;
  label: string;
  sublabel: string;
  link: string;
  sistema: string;
}
export interface UserEmail {
  email: string;
  registration: number;
  contact: string;
}
